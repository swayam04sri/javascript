//let login = 0
///let naame = Boolean(login)
///console.log(naame);
//let num = 33//
//let string_convert = string(num)
//console.log(convert);
//console.log(typeof(convert));
//*****      OPERATIONS */
let a = 6
let neva = -a
console.log(neva);

//About equality
////===
// means it checks datatype Also ==
// only checks the value

// symbol is also primitive data type
const id = Symbol('123')
const anid = Symbol('123')
console.log(id === anid);
// see the difference clearly
//primitive data type stored in stack
//non primitive data type stored in heap